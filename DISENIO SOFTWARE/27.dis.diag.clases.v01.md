# 27.dis.diag.clases.v01.md

## 1. Control de Versiones
| Versión | Fecha | Descripción |
|---|---|---|
| v01 | 23-04-2026 | Versión inicial — Diagrama de Clases |
| v02 | 06-05-2026 | Actualización exhaustiva: Diagrama de Clases Backend y Árbol de Dependencias de React Hooks (Frontend). |

## 1.1 Convención TO-BE vs AS-IS

Este documento se redacta con dos vistas complementarias:

1. **TO-BE (Diseño objetivo):** cómo se define el modelo y los subsistemas a nivel de diseño.
2. **AS-IS (Implementación actual) y mapeo:** cómo se materializa hoy en el código fuente.

Regla: los diagramas y tablas indican explícitamente si son TO-BE o AS-IS para evitar confusiones.

## 2. Modelado de Objetos y Estructuras (Arquitectura Híbrida)

En arquitecturas asíncronas modernas como Tasf.B2B, el "Diagrama de Clases" tradicional se bifurca: el **Backend** obedece a Paradigmas Orientados a Objetos (OOP) estrictos con inyección de dependencias (Spring), mientras que el **Frontend** obedece a un Paradigma Funcional y Reactivo guiado por *Hooks*.

### 2.0 Vista TO-BE (Diseño objetivo)

El diseño objetivo formaliza los subsistemas reales (Simulación+Streaming, Planificador HGA/ALNS y Experimentación Numérica) y define un estado unificado de sesión para consumo UI.

### 2.1 Backend — Paquete de Dominio y Simulación (TO-BE)

| Clase / Entidad | Estereotipo | Atributos/Métodos Críticos | Rol Arquitectónico |
|---|---|---|---|
| `SimulationSessionState` | `@Data` (Objeto en Memoria) | `sessionId`, `status`, `airportLoads`, `activeRoutes` | Actúa como el origen de verdad *Stateful* de una simulación asíncrona particular. |
| `SimulationProgressHolder` | `@Component` (Singleton) | `Map<String, SimulationSessionState> sessions;`<br>`create()`, `get()`, `remove()` | Gestor de concurrencia. Alberga todos los estados activos sin bloquear la base de datos. |
| `SimulationV2Controller` | `@RestController` | `streamSnapshots(sessionId, limit, intervalMs)` | Controlador de borde que emite el objeto `SseEmitter` hacia el exterior. |
| `SimulationService` | `@Service` | `runAsync(sessionId, algorithm)` | Orquestador de negocio. Lanza Hilos separados del API Thread principal. |
| `HGAPlannerService` | `@Service` | `plan(lots, windowMs)`, backup routes | Motor metaheurístico HGA para planificación base y precálculo de rutas de respaldo. |
| `ALNSPlannerService` | `@Service` | `plan(lots, windowMs)`, `replanificar(vueloId, windowMs)` | Motor metaheurístico ALNS para planificación y replanificación ante cancelaciones. |
| `SimulationKpiSnapshotDTO` | DTO (Record / Lombok) | `globalOccupancy`, `airportLoads`, `criticalNodes` | Molde inmutable para transportar métricas por la red. |

### 2.2 Frontend — Árbol de Jerarquía Reactiva (TO-BE)

Debido a que React/Vite descarta el uso de clases UI puras en favor de Hooks Funcionales, documentamos la "jerarquía de clases" como la "Jerarquía de Dependencia de Estados".

| Hook / Componente Funcional | Naturaleza | Dependencias Inyectadas | Propósito Lógico |
|---|---|---|---|
| `api.js` | Helper Funcional | `fetch`, variables de entorno `VITE_API_ORIGIN`. | "Clase Utilitaria" estática. Provee `apiFetch` y unifica la URL base del backend. |
| `useControlTowerController.js` | Custom Hook | `useState`, `useEffect`, `EventSource`, `apiFetch`. | Actúa como el **Controller** principal de MVC. Mantiene el Estado Singleton virtual (rutas y kpis). Engloba la conexión SSE persistente e hidrata el UI. |
| `App.jsx` | Shell / Componente Raíz | `useControlTowerController`, Router | Contenedor de la Torre de Control. Ensambla paneles, escenarios y el mapa. |
| `WorldMap.jsx` | Presenter (Mapa SVG) | `react-simple-maps` | Interpreta el estado espacial (`activeRoutes`, `airportLoads`) y lo dibuja en el mapa. |
| `useNumericExperiment.js` | Custom Hook | `apiFetch` | Aisla la lógica asíncrona y descargas binarias `.xlsx` independientes de la simulación gráfica. |

### 2.3 Enumeraciones del Dominio Estratégico

| Enumeración Backend | Valores Implementados | Contexto de Negocio |
|---|---|---|
| `Status` (TO-BE unificado) | `IDLE`, `RUNNING`, `FAILED`, `DONE` | Estado formal de sesión para UI/telemetría. `IDLE` representa "sin ejecución". |
| `SimulationProgressHolder.Status` (AS-IS) | `RUNNING`, `FAILED`, `DONE` | Implementación actual del simulador: no modela `IDLE` en backend; el idle vive en UI (`simState`). |
| `ExperimentSession.Status` / `ExperimentExportSession.Status` (AS-IS) | `IDLE`, `RUNNING`, `FAILED`, `DONE` | Implementación actual de experimentación numérica y exportación: sí incluye `IDLE`. |
| Tags de escenario (TO-BE) | `VIVO`, `PERIODO`, `COLAPSO` | Etiquetas de UI/negocio para parametrizar ejecución. En AS-IS se materializa como `activeTab` y flags (`isCollapseMode`). |
| Tags de algoritmo (TO-BE) | `HGA`, `ALNS` | Etiqueta para reportes y comparación. En AS-IS se usa como string (`algorithm`) y como `selectedAlgorithm` en frontend. |

### 2.4 Diseño de Patrones Formales
* **Observer:** Visible en el canal EventSource. El Frontend suscribe listeners inmutables (`addEventListener("kpi")`), delegando al Backend la potestad de avisar cambios.
* **Singleton:** `SimulationProgressHolder` es instanciado una sola vez por el contenedor Spring IoC, evitando colisiones de memoria entre llamadas HTTP concurrentes al vuelo.
* **DTO Puros:** `SimulationMapSnapshotDTO` carece de comportamientos. Aislar datos de lógica permite enviar objetos puros de Java directo al Serializador de Jackson hacia JSON para ser consumidos por React.

## 3. Diagramas UML (Mermaid)

Los diagramas se segmentan por subsistema para mantener legibilidad. Cada bloque es autocontenido y puede exportarse a SVG/PNG para insertarse en Word.

### 3.1 Backend — Orquestacion y Estado de Simulacion

```mermaid
classDiagram
direction LR

class SimulationV2Controller <<RestController>> {
  -SimulationProgressHolder progressHolder
  -Executor streamingExecutor
  +getMapSnapshot(sessionId, limit)
  +streamSnapshots(sessionId, limit, intervalMs)
}

class SimulationService <<Service>> {
  -SimulationRunner simulator
  -HGAPlannerService planner
  -ALNSPlannerService alnsPlanner
  -AeropuertoRepository airportRepo
  -SuperLotService superLotService
  -SimulationProgressHolder progressHolder
  -EnvioService envioService
  +runAsync(sessionId, dias, algorithm, startDate)
}

class SimulationProgressHolder <<Component>> {
  +create(sessionId,totalDays)
  +get(sessionId)
  +markDone(sessionId)
  +markFailed(sessionId,errorMessage)
  +saveAlgorithmResult(algorithm,metrics)
  +remove(sessionId)
}

class SimulationSessionState <<Data>> {
  +sessionId: String
  +status: Status
  +percent: int
  +currentDay: int
  +totalDays: int
  +reports: List~SimulationDayReport~
  +airportLoads: Map~String,int~
  +slaPercent: double
  +criticalNodes: int
  +activeRoutes: List~Map~
  +simulatedTime: String
  +currentEpochTime: Long
  +totalBagsWaiting: Integer
  +eventLog: List~String~
  +totalAttended: int
  +totalMissed: int
  +slaFinal: double
  +isCollapseMode: boolean
  +rescuedFlights: int
  +errorMessage: String
  +startEpoch: Long
}

class Status <<enumeration>> {
  IDLE
  RUNNING
  DONE
  FAILED
}

class SimulationRunner <<Component>> {
  -EventEngine engine
  +run(routes, airports, startTime)
}

class EventEngine <<Component>> {
  +buildEvents(routes)
}

class HGAPlannerService <<Service>>
class ALNSPlannerService <<Service>>
class AeropuertoRepository <<Repository>>
class SuperLotService <<Service>>
class EnvioService <<Service>>

SimulationV2Controller --> SimulationProgressHolder
SimulationService --> SimulationRunner
SimulationService --> SimulationProgressHolder
SimulationService --> HGAPlannerService
SimulationService --> ALNSPlannerService
SimulationService --> AeropuertoRepository
SimulationService --> SuperLotService
SimulationService --> EnvioService
SimulationRunner --> EventEngine
SimulationProgressHolder "1" o-- "*" SimulationSessionState : sessions
SimulationSessionState --> Status
```

### 3.2 Backend — Dominio de Rutas y Eventos

```mermaid
classDiagram
direction LR

class Solution {
  routes: List~Route~
  backupRoutes: Map~Integer,Route~
  fitness: double
  routeCount: int
  collapseTime: long
  routesValidas: int
  routesFallidas: int
}

class Route {
  lot: SuperLot
  hops: List~String~
  flights: List~Vuelo~
  demandaTotal: int
  capacidadAsignada: int
  arrivalTime: long
  deadline: long
  status: String
}

class SimulationDayReport {
  dayIndex: int
  startTime: long
  endTime: long
  routes: List~Route~
  colapsed: boolean
  airportSaturation: int
  flightSaturation: int
  collapseTime: long
  slaPercent: double
  totalMaletas: int
  malatetasAtendidas: int
  pendingLots: List~SuperLot~
}

class Event {
  time: long
  type: EventType
  lot: SuperLot
  vuelo: Vuelo
  load: int
}

class EventType <<enumeration>> {
  LOT_ARRIVAL
  FLIGHT_DEPARTURE
  FLIGHT_ARRIVAL
}

class SuperLot {
  id: int
  origenIcao: String
  destinoIcao: String
  totalMaletas: int
  readyTime: long
  sla: long
  intercontinental: boolean
  priority: int
}

class Vuelo <<Entity>> {
  id: Long
  capacidadTotal: Integer
  departureMinute: Integer
  arrivalMinute: Integer
  intercontinental: Boolean
  cancelled: Boolean
}

class Aeropuerto <<Entity>> {
  id: Long
  icaoCode: String
  city: String
  country: String
  continent: Continente
  storageCapacity: Integer
  gmtOffset: Integer
  latitude: Double
  longitude: Double
}

class Continente <<enumeration>>

class SimulationState {
  currentTime: long
  cargaAeropuerto: Map~String,int~
  capacidadVuelo: Map~Long,int~
  saturacionAeropuerto: int
  colapso: boolean
}

Solution "1" o-- "*" Route
SimulationDayReport "1" o-- "*" Route
SimulationDayReport "1" o-- "*" SuperLot
Route "1" o-- "1" SuperLot
Route "1" o-- "*" Vuelo
Event --> EventType
Event --> SuperLot
Event --> Vuelo
Vuelo "*" --> "1" Aeropuerto : origen
Vuelo "*" --> "1" Aeropuerto : destino
SimulationState ..> Aeropuerto
```

### 3.3 Backend — DTOs de Streaming SSE

```mermaid
classDiagram
direction LR

class SimulationMapSnapshotDTO <<DTO>> {
  sessionId: String
  status: String
  simulatedTime: String
  currentEpochTime: Long
  snapshotEpochTime: Long
  activeRoutes: List~SimulationMapRouteDTO~
}

class SimulationMapRouteDTO <<DTO>> {
  id: Integer
  from: String
  to: String
  progress: Double
  status: String
  departureTime: Long
  arrivalTime: Long
  capacityPercent: Double
}

class SimulationKpiSnapshotDTO <<DTO>> {
  sessionId: String
  status: String
  percent: Integer
  currentDay: Integer
  totalDays: Integer
  slaPercent: Double
  globalOccupancy: Double
  criticalNodes: Integer
  airportLoads: Map~String,Integer~
  totalBagsWaiting: Integer
  simulatedTime: String
  currentEpochTime: Long
  isCollapseMode: Boolean
  rescuedFlights: Integer
  errorMessage: String
}

SimulationMapSnapshotDTO "1" o-- "*" SimulationMapRouteDTO
```

### 3.4 Backend — Planificador (HGA/ALNS) y Replanificación

```mermaid
classDiagram
direction LR

class PlanningController <<RestController>> {
  -PlanningService planningService
  +ejecutarHGA(windowMs)
  +ejecutarALNS(windowMs)
  +replanificar(vueloId, windowMs)
  +getSolucionActual()
}

class PlanningService <<Service>> {
  -SuperLotService superLotService
  -VueloService vueloService
  -HGAPlannerService hgaPlanner
  -ALNSPlannerService alnsPlanner
  -PlanningSessionHolder sessionHolder
  +ejecutarHGA(windowMs)
  +ejecutarALNS(windowMs)
  +replanificar(vueloId, windowMs)
  +getSolucionActual()
}

class PlanningSessionHolder <<Component>> {
  -currentSolution: Solution
  +store(sol)
  +get()
  +hasSolution()
  +clear()
}

class HGAPlannerService <<Service>> {
  -RouteBuilder routeBuilder
  -FitnessEvaluator fitness
  -SimulationRunner simulator
  +plan(lots, ignored, windowMs)
}

class ALNSPlannerService <<Service>> {
  -RouteBuilder routeBuilder
  -FitnessEvaluator fitnessEval
  -SimulationRunner simulator
  -PlanningSessionHolder sessionHolder
  +plan(lots, windowMs)
  +replanificar(vueloIdCancelado, windowMs)
}

class RouteBuilder <<Component>> {
  -NetworkAdapter network
  +build(lot, airportMap, cargaAeropuerto, capacidadVuelo)
  +buildBackup(lot, airportMap, excludedFlightIds)
}

class NetworkAdapter <<Interface>>
class NetworkAdapterImpl <<Component>>
class FitnessEvaluator <<Component>>

class VueloService <<Service>> {
  +cancelarVuelo(vueloId)
}

class SuperLotService <<Service>>
class SimulationRunner <<Component>>

PlanningController --> PlanningService
PlanningService --> SuperLotService
PlanningService --> VueloService
PlanningService --> HGAPlannerService
PlanningService --> ALNSPlannerService
PlanningService --> PlanningSessionHolder

HGAPlannerService --> RouteBuilder
HGAPlannerService --> FitnessEvaluator
HGAPlannerService --> SimulationRunner

ALNSPlannerService --> RouteBuilder
ALNSPlannerService --> FitnessEvaluator
ALNSPlannerService --> SimulationRunner
ALNSPlannerService --> PlanningSessionHolder

RouteBuilder --> NetworkAdapter
NetworkAdapterImpl ..|> NetworkAdapter
```

### 3.5 Backend — Experimentación Numérica (DoE) y Exportación Excel

```mermaid
classDiagram
direction LR

class NumericExperimentController <<RestController>> {
  -NumericExperimentService experimentService
  -ExcelExportService excelExportService
  +getDOELevels()
  +startExperiment(payload)
  +getStatus(sessionId)
  +startExport(payload)
  +getExportStatus(exportSessionId)
  +downloadExport(exportSessionId)
}

class NumericExperimentService <<Service>> {
  +calculateDOELevels()
  +createSession(algorithm, levels)
  +runExperiment(sessionId)
  +getSession(sessionId)
}

class ExcelExportService <<Service>> {
  +createExportSession(algorithm, levels)
  +runExport(exportSessionId, levels)
  +getExportSession(exportSessionId)
}

class ExperimentSession {
  +id: String
  +status: Status
  +progressPercent: int
  +algorithm: String
  +levels: List~LevelDefinition~
}

class ExperimentExportSession {
  +id: String
  +status: Status
  +progressPercent: int
  +currentIteration: int
  +currentLevel: int
  +fileName: String
  +filePath: String
  +errorMessage: String
}

class ExperimentSessionStatus <<enumeration>> {
  IDLE
  RUNNING
  DONE
  FAILED
}

NumericExperimentController --> NumericExperimentService
NumericExperimentController --> ExcelExportService
NumericExperimentService --> ExperimentSession
ExcelExportService --> ExperimentExportSession
ExperimentSession --> ExperimentSessionStatus : status
ExperimentExportSession --> ExperimentSessionStatus : status
```

## 4. Referencias
* Componentes de software: `DISENIO SOFTWARE/25.dis.componentes.v01.md`
* Arquitectura: `DISENIO SOFTWARE/24.dis.arq.solucion.v01.md`
